# notes for rebase conflict

# 1. solve the conflict and save the file

# 2. run add
git add .
# Do NOT run git commit after add(Not like merge conflict)

# 3A. continue rebase
git rebase --continue

# 3B. abort rebase
git rebase --abort
